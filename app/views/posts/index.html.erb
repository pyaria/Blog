<div class="container-fluid">
<h1>All your blog posts</h1>
  <%= @message %>
  <% @p.each do |p|%>
    <div>
      <%= link_to p.title, post_path(p) %>
    </div>
  <% end %>
  <%= paginate @p %>
</div>
